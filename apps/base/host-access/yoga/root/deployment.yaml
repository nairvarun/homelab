---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: yoga-root
  namespace: host-access
spec:
  selector:
    matchLabels:
      name: yoga-root
  template:
    metadata:
      labels:
        name: yoga-root
    spec:
      hostNetwork: true
      hostPID: true
      hostIPC: true
      containers:
      - name: yoga-root
        image: ubuntu:latest
        command: ["sleep", "infinity"]
        stdin: true
        tty: true
        securityContext:
          privileged: true
        volumeMounts:
        - name: host
          mountPath: /host
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
      volumes:
      - name: host
        hostPath:
          path: /home/nv
      restartPolicy: Always